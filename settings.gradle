pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'agent-docs'

include(
    'agent-docs-publish-gradle-plugin',
    'agent-docs-resolve-gradle-plugin',
    'agent-docs-integration-tests'
)
